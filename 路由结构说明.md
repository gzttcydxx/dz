# 路由结构说明

## 路由分包
- `routes/`: 路由蓝图包
  - `__init__.py`: 导出所有蓝图
  - `main_routes.py`: 主页相关路由（`/`）
  - `auth_routes.py`: 认证相关路由（注册、登录、登出）
  - `room_routes.py`: 房间与页面路由（创建房间、大厅、游戏、加入房间）
  - `equipment_routes.py`: 装备相关路由（角色装备、装备强化）
  - `gacha_routes.py`: 抽卡页面与抽卡接口

## 规范
- 每个文件包含完整路由与视图函数，使用 `Blueprint` 管理。
- 保持原有端点和参数不变：`index` 端点兼容 `url_for('index')`。
- `main.py` 负责应用初始化与蓝图注册，业务路由全部在 `routes` 包内。

## 导入路径
- 蓝图文件中从 `main` 导入共享数据与工具函数，例如：`rooms`、`load_user_data`。
- 战斗配置从 `game_combat` 导入：`ENEMY_CONFIG`、`ATTRIBUTE_ADVANTAGE`。

## 验证
- 运行应用，访问所有原有端点应保持功能不变。
- 通过 `pytest` 可对路由进行基本可达性测试。